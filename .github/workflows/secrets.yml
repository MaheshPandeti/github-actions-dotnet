name: Secrets workflow
on:
  workflow_dispatch:
env:
  API_CSPROJ_PPATH: ".src/GitHubActionsDotNet.Api/GitHubActionsDotNet.Api.csproj"
jobs:
  secrets:
    name: Secrets Demo
    runs-on: ubuntu-latest

    steps:    
     - name: Run environment variable
       run: echo "$API_CSPROJ_PPATH"
